x-aion-runtime-env: &aion-runtime-env
  APP_ENV: production
  APP_PORT: 8000
  LOG_LEVEL: INFO
  OPENAI_API_KEY: ${OPENAI_API_KEY}
  OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
  TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
  TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET}
  DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://aion:aion@db:5432/aion}
  REFLECTION_RUNTIME_MODE: ${REFLECTION_RUNTIME_MODE:-deferred}
  SCHEDULER_EXECUTION_MODE: ${SCHEDULER_EXECUTION_MODE:-externalized}
  PROACTIVE_ENABLED: ${PROACTIVE_ENABLED:-false}
  REFLECTION_INTERVAL: ${REFLECTION_INTERVAL:-900}
  MAINTENANCE_INTERVAL: ${MAINTENANCE_INTERVAL:-3600}
  PROACTIVE_INTERVAL: ${PROACTIVE_INTERVAL:-1800}

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment: *aion-runtime-env
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  maintenance_cadence:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment: *aion-runtime-env
    depends_on:
      db:
        condition: service_healthy
    command:
      [
        "sh",
        "-lc",
        "while true; do python scripts/run_maintenance_tick_once.py; sleep ${MAINTENANCE_INTERVAL:-3600}; done",
      ]

  proactive_cadence:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment: *aion-runtime-env
    depends_on:
      db:
        condition: service_healthy
    command:
      [
        "sh",
        "-lc",
        "while true; do python scripts/run_proactive_tick_once.py; sleep ${PROACTIVE_INTERVAL:-1800}; done",
      ]

  db:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_DB: aion
      POSTGRES_USER: aion
      POSTGRES_PASSWORD: aion
    volumes:
      - aion_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aion -d aion"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  aion_db_data:
